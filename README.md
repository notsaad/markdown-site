# markdown personal site
I wanted a simple personal site written in markdown so it is quick and lightweight

## how to run locally

```bash
# Install dependencies
npm install

# Development (watch mode)
npm run dev

# In another terminal, serve the site
npm run serve

# Or just build once
npm run build
```
